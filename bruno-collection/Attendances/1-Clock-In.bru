meta {
  name: Clock In
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/Attendances/clock-in
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "employeeId": 1,
    "dateTime": "{{$datetime}}",
    "notes": "Clock in via API"
  }
}

