meta {
  name: Logout
  type: http
  seq: 7
}

post {
  url: {{base_url}}/api/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("Logout message is returned", function() {
    expect(res.body.message).to.exist;
  });
}

